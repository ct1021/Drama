# 轻量 Boxing 100K 启动记录

正式启动：2026-09-21T10:33:35.435940+08:00。当前启动验收到 1400 步，进程 PID 4119，世界模型和策略均已更新，损失为有限值。此文件是启动快照，实时进度以后续小时记录为准。

- 唯一运行：boxing-lightweight-baseline-s3710-100k-20260921；ALE/Boxing-v5；seed 3710；目标 100000 次环境交互。
- 世界模型 7,161,603 参数；策略 3,419,664 参数。五级编解码器 [1,2,3,4,4]，原 Mamba2、DFS 与策略训练保留。head_dim=64，与论文参数表并非完全一致。
- 训练代码固定 c2a8106e82cd0ca577d3f1527061ddbabb4473f6，分支 research/atari100k-baseline。启动记录的后续提交不部署到运行中的代码目录。
- 每 10K 用固定 10 回合完整评测；保存逐回合回报。预计 5–6 GPU 小时，7.9 小时主动退出、8 小时外层进程上限。上限不停止平台计费。
- 数据盘当前剩余 34.79 GiB。新实例价格未提供，不沿用旧实例单价计算实际费用。
- 前置 1200 步集成完成，2 回合回报 [-38,-38]，模型与策略更新和权重落盘通过，显存峰值 5.52 GiB。短测试仅是工程检查。

## 为什么运行

先建立与后续架构试验一致的轻量基线，避免拿 21.93M 旧模型与 7.16M 新模型直接比较而混淆容量影响。本次没有实现 H1，不能据此判断架构假说有效、泛化改善或完整论文复现。后续 H1 仍需落实结构和对照，提出的四组总预算约 22–28 GPU 小时，未启动后续队列。

## 保存与检查

配置、manifest、启动状态和完整短测试指标已进入仓库分支。本次长训练的日志、指标及检查点仍在远端增长，尚未完成离线归档，当前不能据此释放实例。完成或失败后下载并验证，再更新归档状态。权重不含优化器、回放和 RNG 状态，不支持精确续训。

远端运行目录：/root/autodl-tmp/drama-research-20260919/runs/boxing-lightweight-baseline-s3710-100k-20260921。
