# H1 两组100K启动与小时检查

2026-09-21 16:58（UTC+8）。用户在架构方案之后要求继续；本次只执行既定C/D两组，不恢复旧矩阵。

## 假说与运行范围

不同预测任务可能需要不同历史信息更新路径。比较C“只在输出分工”和D“递归内部持续分工”，检验持续路由的额外价值。A为已完成的7.16M轻量基线，最终10回合61.0。

| 组 | profile | 世界模型参数 | 唯一运行目录名 |
|---|---|---:|---|
| C 输出对照 | lightweight-readout | 6,964,995 | boxing-readout-control-s3710-100k-20260921 |
| D H1路由 | lightweight-routed | 5,762,819 | boxing-h1-routed-s3710-100k-20260921 |

均为Boxing、seed3710、100000次环境交互、每10K及终点10回合greedy评测。C先执行，只有C成功100K后才执行D。每组合作上限6.9小时、硬上限7小时；总监督上限14小时。失败不重试，不增加B/E/种子。不安装依赖、不操作实例租借/续费/关停。运行上限不会停止平台计费；当前实例单价和总预算未提供。

## 验收与版本

先读26-H1完整结构与短训练验收.md。A/C/D三组1800步均完成，D完整更新测速相对A +3.79%，通过事先15%门槛。GPU缓存、并行/单步、决策AMP、CUDA Graph想象、有限梯度检查通过；交互协议回归也在现有环境通过。此处短训练不构成RL性能证据。

研究分支 research/atari100k-baseline；训练代码固定5829fc831febdbd844506f9d87906e6c0b348cbb。验收记录及两组执行器已推送dedc5e6。候选目录独立，原code目录仍c2a8106。

远端根目录 `/root/autodl-tmp/drama-research-20260919`；候选代码 `candidates/5829fc831febdbd844506f9d87906e6c0b348cbb`；所有上述运行在根目录的runs下。

外部执行器 `exports/h1-pair-executor.py` SHA256：`7a2d380c1f3c515bd03ded9f4c3c7224d21f16915dddff81e45fe2eabc062025`，外层监督PID15027。协调状态 `runs/h1-pair-s3710-20260921/pair-status.json`，协调日志 `exports/h1-pair-console.log`。进程身份应每次实时复核，不能只凭PID存活判断。

## 每小时检查

只使用本机 `C:/Users/CT1021/.ssh/drama-research/clone-20260921/connection.json` 和配置指定私钥，known_hosts使用clone目录。BatchMode=yes、StrictHostKeyChecking=yes。不得读取或输出密码、私钥，不使用旧实例连接。

本地入口：`environment-clone-20260921/h1_hourly_check.py`。该脚本只读远端两组和协调状态，保存到 `environment-clone-20260921/h1/hourly-checks/`。远端使用 `envs/drama-cu128/bin/python` 执行候选目录 `ops/check_run.py`，参数只允许上述两个完整运行路径，同时检查数据盘余量。网络/查询失败只重试一次，不直接判定训练失败。

每小时3–5条简报：当前组、步数/100K/百分比及较上次增量；最新完整10回合评测；模型/策略损失有限性与报错；预计剩余时间、磁盘余量。未产生损失或评测时明确“尚未产生”。阶段分数不能当终点效果。C完成/D开始、失败、停滞、磁盘不足要说明。

任一组完成后及时下载配置、manifest、全部评测/指标/日志和最终模型/策略权重，逐项核对大小与SHA256；沿用已完成轻量基线的归档、Release分块及公开下载恢复验证流程，创建独立归档，旧Release保持不变。可先完成C归档，同时继续监控D；两组都终态并保留证据后暂停检查。若失败/超时，保留现有证据与实际步数，停止扩大实验；已完成C仍须归档。不能自动重启或修改运行代码/模型/参数。

## 判断与交付

按25号规格：D的100K终点和60–100K均分都优于A/C且效率门槛通过，才支持扩大验证；两项均不提高则不扩大，一项提高为混合证据。参数不匹配，容量/稀疏正则化与损失协调尚未排除；本轮不是完整论文复现或已成立创新。现有权重不含优化器/回放/RNG，不支持精确续训。

预计两组约10–12 GPU小时，按16:58启动约在9月22日03:00–05:00取得结果（随实测更新），最迟外层07:00前终止。9月24日材料以架构动机、A/C/D完整曲线、终点评测与资源为核心，明确负结果与未排除解释。新运行尚未归档，不可将旧基线迁移验收套用到C/D。
