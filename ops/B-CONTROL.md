# B 优化对照：授权范围与实现

用户于2026-09-21明确允许在D完成后加入B。仅Boxing/seed3710/100K；GPU短验收15分钟，正式硬上限7小时。不自动重试或增加种子。

沿用 `experiments/plans/20260921-B-optimization-proposal.md` 的公式：图像、奖励、KL（动力学+0.1表征）三项各乘exp(-s)，三个s从0开始，加入softplus(s)，终止损失保持权重1。保留原KL目标、detach、free bits和模型配置。标量与世界模型同学习率/调度、无weight decay；梯度裁剪与模型参数共同计算范数。损失/系数/正则分别记录，B遇非有限梯度停止。A/C/D默认路径不开此功能。

此为HarmonyDream方法在Drama上的优化对照，不是完整HarmonyDream复现；不作为我们的架构创新。预测网络和初始化不变，预计7,161,606个世界模型参数（含3个训练标量）。不读取C/D权重续训。

## 接续

`run_b_after_d.py`固定等待 `boxing-h1-routed-s3710-100k-20260921`：D正确源码/环境/种子、完成100K、10回合终点评测、进程退出、C/D协调器completed、GPU空闲及磁盘足够才继续。D失败或到其7小时上限后仍未成功，不启动B。

先固定D终点文件大小/哈希，避免慢速公开下载阻塞GPU接续；D完整归档由监控继续执行。随后 `validate_b_stage.py` 在外部15分钟硬限内依次执行：CPU回归、实际GPU数值/缓存/想象/优化器检查、B1800步真实短训练及评测。检查训练标量确实更新且保存到权重；1200–1700步完整更新耗时相对原A窗口增加不超过15%。预设门槛不随结果调整。

所有门槛通过才创建唯一正式目录 `boxing-optimization-control-s3710-100k-20260922`，从头100K，10K间隔10回合评测。失败不重开，保留日志。预检属于工程证据，不能当RL结果。

## 已完成本地检查与未完成项

- 5项CPU损失测试：初始模型梯度、标量更新/恢复、优化器覆盖与weight decay、初始化RNG、softplus数值稳定。
- 5项profile隔离测试：B仅在轻量配置上开启损失协调，A/C/D配置不变。
- 3项门槛测试：只接受准确100K、15%效率阈值、非有限指标/未更新标量拒绝。
- GPU完整模型参数、初始化、缓存和想象验证尚待D结束，在服务器前置门槛执行。未用本地CPU测试冒充GPU验收。
- 本机Torch/NumPy接口有版本提示，但上述纯Torch CPU测试退出0；未改变任何依赖，服务器既有环境另行执行回归。

正式训练源码及接续进程以部署记录为准。仅B完成并迁移校验后暂停扩大后的监控；原C/D完成后暂停的规则已被此次B授权替代。
