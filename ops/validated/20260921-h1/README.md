# H1完整接入与短训练验收

2026-09-21。H1-R1已经接入世界模型构造与三种执行路径，配对输出侧对照已实现；本文件不代表100K架构收益结果。

## 已完成

- 本地路由组件6项、配置选择4项测试通过；GPU在既有5090环境验证，无安装依赖。
- 3组均通过并行/缓存单步一致性、1/8步预填充覆盖旧缓存、相同AMP精度的真实决策一致性、有限梯度与优化器参数覆盖检查。
- D跨层SSM和卷积私有状态扰动的两个非目标输出差异均为0；目标分区有变化。此结论限固定输入和动作。
- 原默认CUDA Graph想象1024×16通过。含卷积的float32递归缓存均为164864字节/样本。
- A/C/D都实际运行到1800次交互并完成2回合评测、保存终点权重。短测试得分不作为RL性能证据。
- 三组源代码固定5829fc831febdbd844506f9d87906e6c0b348cbb；原基线代码目录保持c2a8106，使用独立候选目录。

## 实际完整更新耗时

以1200–1700步同一指标时间戳统计，包含真实交互、世界模型、想象和策略更新；排除初始预热和最后评测。只有一个500步窗口，不能承诺100K全程严格同比。

| 配置 | 世界模型有效参数 | 每交互秒数 | 相对A耗时 | PyTorch峰值GiB |
|---|---:|---:|---:|---:|
| lightweight | 7,161,603 | 0.1729 | 1.000 | 5.521 |
| lightweight-readout | 6,964,995 | 0.1700 | 0.983 | 5.520 |
| lightweight-routed | 5,762,819 | 0.1795 | 1.038 | 5.502 |

D较A耗时变化 **3.79%**。原预设不超过15%的短训练效率门槛：**通过**。不更改门槛。

## 科研判断

这是一个已实现并通过工程检查的候选结构，不是创新性或回报提升证明。C与D参数不同；D并非7.16M等参数模型。正式结果即使提高，也需继续排除容量变化、稀疏正则化和损失协调等替代解释。
下一阶段限既定C/D两组单种子100K对照，B/E和新增种子不自动排队。短训练门槛允许进入该阶段，但100K运行是否已启动以单独启动记录为准。
训练权重不含优化器/回放/RNG状态，不声称精确续训。
