# H1 路由原型：当前验证范围

2026-09-21。`h1_routing.py` 实现只注册允许连接的分块线性层及按区 RMS 归一化。
它尚未接入 Mamba2、训练器、想象或真实决策，不能通过现有训练 profile 启动。
研究依据及正式实验判断条件见 `experiments/plans/20260921-h1-research-decision.md`。

本地 CPU `python ops/prototypes/test_h1_routing.py`：5 项通过。
覆盖跨层/残差/分区归一化的输入扰动隔离、允许及禁止梯度、全局归一化泄漏的负对照、
实际参数计数、重复角色打包/状态保存加载、非法布局。它们不是强化学习或记忆收益实验。
未覆盖真正的 SSM/卷积缓存、GPU融合路径、并行与单步推理一致性、CUDA Graph 或端到端速度。

实现通过拼接构造稠密权重供线性运算，虽然不注册禁止连接的自由参数，仍执行稠密矩阵乘法并产生临时张量；
不能称稀疏加速。接入时还须检查优化器分组、初始化、混合精度和融合算子的权重访问路径。

本机 PyTorch 导入/随机数初始化提示既有 NumPy 1.x/2.x ABI 不兼容；这些纯张量测试未调用 NumPy，
5 项测试正常结束。没有安装、升级、降级任何依赖，不能据此声称本机完整训练环境可用。

下一步是实现与验证完整 SSM 依赖路径并制作仅输出分工的配对对照；不自动发起长训练。
